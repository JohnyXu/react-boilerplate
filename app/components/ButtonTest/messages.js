/*
 * ButtonTest Messages
 *
 * This contains all the text for the ButtonTest component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ButtonTest';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ButtonTest component!',
  },
});
